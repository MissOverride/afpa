#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../aggregate-commands/bin/aggregate-commands" "$@"
  ret=$?
else 
  node  "$basedir/../aggregate-commands/bin/aggregate-commands" "$@"
  ret=$?
fi
exit $ret
